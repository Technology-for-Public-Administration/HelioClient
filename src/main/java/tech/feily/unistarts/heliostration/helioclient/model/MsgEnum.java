package tech.feily.unistarts.heliostration.helioclient.model;

public enum MsgEnum {

    hello, detective, confirm,
    init, service, note,
    update, close, error,
    exception, request, reply,
    prePrepare, prepare,
    commit;
    
}
